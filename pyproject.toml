[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "starsolver"
version = "0.0.1"
requires-python = ">=3.9"

dependencies = [
    "numpy",
    "Pillow",
    "tetra3",
]

[project.scripts]
starsolver = "starsolver.pipeline:main"